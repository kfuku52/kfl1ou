% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placeholder_outputs.R
\name{write_l1ou_placeholder_outputs}
\alias{write_l1ou_placeholder_outputs}
\title{Writes placeholder outputs for workflows expecting l1ou-style files}
\usage{
write_l1ou_placeholder_outputs(
  tree,
  Y,
  dir = ".",
  prefix = "l1ou",
  plot.width = 8,
  plot.height = NULL,
  reason = NULL
)
}
\arguments{
\item{tree}{tree of class \code{phylo}.}

\item{Y}{trait matrix or data frame aligned to \code{tree$tip.label}.}

\item{dir}{output directory.}

\item{prefix}{output file prefix. The default \code{"l1ou"} writes files such
as \code{l1ou_tree.tsv}.}

\item{plot.width}{width of the placeholder PDF in inches.}

\item{plot.height}{optional height of the placeholder PDF in inches. When
\code{NULL}, the height scales with the number of tips.}

\item{reason}{optional character string stored in the returned metadata.}
}
\value{
Invisibly returns a list containing placeholder tables, file paths,
and the optional \code{reason}.
}
\description{
This helper writes placeholder TSV and PDF outputs for workflows that expect
\code{l1ou_tree.tsv}, \code{l1ou_regime.tsv}, \code{l1ou_leaf.tsv}, and a
simple tree plot even when no shifts can be fitted, for example because all
traits are invariant.
}
\examples{
data(lizard.tree, lizard.traits)
keep <- lizard.tree$tip.label[1:8]
tree <- ape::drop.tip(lizard.tree, setdiff(lizard.tree$tip.label, keep))
Y <- as.matrix(lizard.traits[keep, 1, drop = FALSE])
Y[] <- 1
out.dir <- tempdir()
write_l1ou_placeholder_outputs(tree, Y, dir = out.dir)
}
